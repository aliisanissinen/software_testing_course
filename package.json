{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "nyc — -reporter=html — reporter=text mocha — timeout 10000 — exit — require babel-register",
    "coverage": "nyc report — -reporter=text-lcov | coveralls"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.1.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0"
  },
  "dependencies": {
    "chai": "^4.3.7",
    "eslint": "^8.28.0",
    "mocha": "^10.1.0"
  }
}
